import MapView, {
  Circle,
  Marker,
  Polyline,
  PROVIDER_GOOGLE,
} from "react-native-maps";

export type AppMapRef = MapView;

export { Circle, Marker, Polyline, PROVIDER_GOOGLE };
export default MapView;
